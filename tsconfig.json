{
  "$comment": [
    "The catalog's compile gate, and the only thing in this repo that needs an install.",
    "Generating Loom is pure Node with no dependencies; this exists so `tsc --noEmit` can",
    "answer whether the atoms actually compile, which no read of the emitted CSS can.",
    "tsconfig.scripts.json is the other half: the generator's own modules, checked from",
    "their JSDoc."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "types": ["react"]
  },
  "include": ["catalog/**/*.ts", "catalog/**/*.tsx"]
}
