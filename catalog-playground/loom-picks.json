{
  "$schema": "Loom per-project picker. setup.sh reads `picks`, resolves each atom's manifest `dependencies` transitively, and copies only the resulting set (+ cn) into src/components/. Re-run setup.sh to resync.",
  "loom": {
    "picks": [
      "badge",
      "button",
      "dialog",
      "form-field",
      "select"
    ]
  }
}
